# Sales Analytics System

This project is a simple **sales analytics system** that reads sales data, processes it, and can interact with external APIs.

## Setup

1. Make sure you have Python 3 installed.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
